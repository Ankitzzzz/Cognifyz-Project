{
  "name": "frontend",
  "version": "0.1.0",
  "description": "Frontend static UI for forms demo",
  "scripts": {
    "start": "npx serve -s . -l 5000",
    "dev": "npx serve -s . -l 5000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
